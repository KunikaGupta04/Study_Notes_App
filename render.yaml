services:
  - type: web
    name: smartnotes-ai
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get install -y ffmpeg
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production